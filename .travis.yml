language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulpy' ]; then cd .. && eval "mv $currentfolder generator-gulpy" && cd generator-gulpy; fi

